#define _CRT_SECURE_NO_WARNINGS
#include<cstdio>
#include<iostream>
#include<iomanip>
#include<cstdlib>
#include<cmath>
#include<cstring>
#include<string>
#include<vector>
#include<stack>
#include<queue>
#include<map>
#include<set>
#include<algorithm>
#include<utility>
#include<functional>
#define For(i, n) for(int i = 0; i < (n); ++i)
#define Ri(x) scanf("%d",&(x))
#define R2i(x,y) scanf("%d %d",&(x),&(y))
#define Pi(x) printf("%d",(x))
#define Piln(x) printf("%d\n",(x))
using namespace std;

int main()
{
    while(0){
	For(i,n);
	int x,y;
	Ri(x);
	R2i(x,y);
	Pi(x);
	Piln(x);
    }
    return 0;
}
